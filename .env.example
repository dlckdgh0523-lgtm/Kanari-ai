# 이 파일을 복사해서 .env 를 만들고 값을 채운다. .env 는 절대 커밋하지 않는다.
PORT=3000

DB_HOST=localhost
DB_PORT=3306
DB_USER=root
DB_PASSWORD=kanari-dev
DB_NAME=kanari

KAFKA_BROKER=localhost:9092

# 프로젝트별 웹훅이 없을 때 쓰는 기본 Discord 웹훅 (비워두면 알람 생략)
DISCORD_WEBHOOK_URL=
